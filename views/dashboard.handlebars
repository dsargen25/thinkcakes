{{> nav/nav-block}}

<main>
    <div class="home-previews">
      <div class="cake-preview cakes">
        {{#each cakes}}
        <div class="cake-imgcontainer"><img src="{{cakeimageUrl}}"></div>
        <div class="cake-textcontainer">
          <p>{{name}}</p>
          <p>{{difficulty}}</p>
          <p>{{cakeLikes}}</p>
        </div>
        {{else}}
        <div>
          <p>No cakes available</p>
        </div>
        {{/each}}
      </div>
    </div>
</main>
{{> modal/login-block}}
